#####Feeds#####

#auto-reload yes

#####Colors#####



include /usr/share/doc/newsboat/contrib/colorschemes/solarized-dark
#default:
color listnormal_unread yellow default
#color listfocus_unread blue default reverse bold
#color info default default reverse
#color listfocus blue default reverse bold
#color background          default   default
#color listnormal          default   default
#color article             default  default 

text-width 80

#####Navigation#####

browser "firefox --new-tab %u"

#vim keys
bind-key j down
bind-key k up
bind-key l open
bind-key h quit

#arrows
bind-key RIGHT open
bind-key LEFT quit

bind-key q hard-quit

#####Macros#####

macro y set browser "setsid -f celluloid --enqueue"; open-in-browser; set browser "firefox --new-tab %u"
macro d set browser "tilix -a app-new-session -e youtube-dl"; open-in-browser; set browser "firefox --new-tab %u"
macro p set browser "tilix -a app-new-session -e mpv --vid=no"; one; set browser "firefox --new-tab %u"

#####Podboat####

download-path "~/Documents/InternalHD/Documents/Podcasts/%n"
download-filename-format "%F-%t.%e"
